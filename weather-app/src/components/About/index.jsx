import React from 'react';
import PropTypes from 'prop-types';

About.propTypes = {
    
};

function About(props) {
    return (
        <div>
            this is about page
        </div>
    );
}

export default About;